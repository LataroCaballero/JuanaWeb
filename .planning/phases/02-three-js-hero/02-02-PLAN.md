---
phase: 02-three-js-hero
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: false
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
must_haves:
  truths:
    - "The 3D logo is visually correct — circle with arrow-eyes, Electric Blue chrome material"
    - "The H1 is visible and readable, not obscured by the canvas overlap"
    - "The idle animation (float + rotation) runs smoothly"
    - "No layout shift visible when the page loads"
  artifacts: []
  key_links: []
---

<objective>
Verify the Three.js hero visually and functionally in the browser. Confirm the 3D logo renders correctly, the layout works on desktop and mobile, the animation is smooth, and there are no performance regressions.

Purpose: Human eyes confirm what automated checks cannot — visual quality, animation feel, layout balance.
Output: Approved hero section ready for Phase 3.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-three-js-hero/02-01-SUMMARY.md
@.planning/phases/02-three-js-hero/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks</name>
  <files></files>
  <read_first>
    - src/scripts/hero-canvas.ts (verify dispose sequence)
    - src/components/HeroCanvas.astro (verify static H1 and aspect-ratio)
    - src/pages/index.astro (verify HeroCanvas is wired)
  </read_first>
  <action>
    Run the following verification sequence:

    1. `npm run build` — must exit 0 with no errors
    2. Check the built HTML output for static h1 content:
       `grep -r "COFFEE ON YOUR WAY" dist/` — must find the h1 in the static HTML output
    3. Check that Three.js is NOT inlined in the main page HTML (it should be a separate chunk loaded dynamically):
       `grep -L "THREE" dist/index.html` — if index.html does NOT contain THREE, the dynamic import is working
    4. Verify the dispose sequence in hero-canvas.ts:
       - `forceContextLoss` appears BEFORE `dispose` in the file (check line numbers)
    5. Start dev server: `npm run dev` (keep running for the checkpoint)
  </action>
  <verify>
    <automated>npm run build && grep -q "COFFEE ON YOUR WAY" dist/index.html && echo "H1 in static HTML: OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `dist/index.html` contains the string `COFFEE ON YOUR WAY` (LCP element is in static HTML)
    - Dev server starts without errors on `npm run dev`
  </acceptance_criteria>
  <done>Build succeeds and static HTML contains the LCP h1 element</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of 3D hero</name>
  <files></files>
  <action>
    Present the running dev server to the user for visual verification. The user will check the hero section at http://localhost:4321 in Chrome across desktop and mobile viewports.
  </action>
  <verify>
    <automated>echo "Human verification — no automated check"</automated>
  </verify>
  <what-built>
    Complete Three.js hero section with:
    - Static H1 "COFFEE ON YOUR WAY" as LCP element
    - 3D smiley logo (circle + arrow-eyes) in Electric Blue chrome material
    - Idle float + rotation animation
    - Desktop 2-column grid layout with -48px canvas overlap
    - Mobile stacked layout with -24px canvas overlap
  </what-built>
  <how-to-verify>
    1. Open http://localhost:4321 in Chrome
    2. **Visual check — Desktop:**
       - H1 "COFFEE ON YOUR WAY" is visible on the left in uppercase, large text
       - 3D logo is on the right, slightly overlapping the h1 column (Organic Brutalism)
       - Logo is Electric Blue (#0055ff) with metallic chrome-like sheen
       - Logo shape: circle with two arrow-shaped eyes
       - Logo slowly rotates and floats up/down (subtle, breath-like)
       - Background is dark (#131313) with grain texture overlay visible on top
    3. **Visual check — Mobile:** Resize browser to less than 768px width
       - H1 is on top, canvas below
       - Canvas overlaps upward slightly (-24px)
       - Logo scales appropriately (70vw width)
    4. **Performance check:**
       - Open DevTools > Network tab, reload page
       - Three.js chunk should load as a separate JS file (not inlined)
       - No layout shift visible during load (CLS = 0)
    5. **Animation check:**
       - The logo should float smoothly — no jank or stuttering
       - In Chrome DevTools, go to Rendering > check "Emulate CSS media: prefers-reduced-motion: reduce", reload
       - The logo should be static (no animation) but still rendered
    6. **If anything looks wrong:** Describe the issue (e.g., "logo is upside down", "eyes not visible", "animation too fast")
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe what needs fixing</resume-signal>
  <done>User has visually verified the 3D hero section works correctly</done>
</task>

</tasks>

<verification>
Human confirms visual quality and animation behavior in browser.
</verification>

<success_criteria>
- User approves the visual appearance of the 3D hero
- Layout works on both desktop and mobile viewports
- Animation is smooth and subtle
- No visible layout shift on page load
- Reduced motion preference correctly disables animation
</success_criteria>

<output>
After completion, create `.planning/phases/02-three-js-hero/02-02-SUMMARY.md`
</output>
