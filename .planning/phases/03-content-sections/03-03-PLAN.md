---
phase: 03-content-sections
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - src/components/FooterSection.astro
  - src/pages/index.astro
autonomous: false
requirements: [FOOT-01]

must_haves:
  truths:
    - "Footer shows 'JUANA HOUSE' in Electric Blue, Instagram @juana.onyourday link, copyright, and tagline chip"
    - "All five sections are visible in order: Nav (fixed) → HeroCanvas → HistoriaSection → UbicacionesSection → MarqueeSection → FooterSection"
    - "Nav is outside <main>, FooterSection is outside <main> — semantic HTML structure correct"
    - "npm run build completes without errors after all components are wired"
    - "The full page is navigable: clicking 'NUESTRA HISTORIA' scrolls to #historia, clicking 'UBICACIONES' scrolls to #ubicaciones"
  artifacts:
    - path: "src/components/FooterSection.astro"
      provides: "Footer with JUANA HOUSE logotype, Instagram link, copyright, tagline"
      exports: []
    - path: "src/pages/index.astro"
      provides: "Wired page: Nav + main[HeroCanvas + Historia + Ubicaciones + Marquee] + Footer"
      exports: []
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/Nav.astro"
      via: "<Nav /> placed before <main> inside BaseLayout slot"
      pattern: "import Nav.*Nav.astro"
    - from: "src/pages/index.astro"
      to: "src/components/FooterSection.astro"
      via: "<FooterSection /> placed after </main> inside BaseLayout slot"
      pattern: "import FooterSection.*FooterSection.astro"
    - from: "Nav anchor #historia"
      to: "HistoriaSection id=historia"
      via: "index.astro wiring — section must be in the DOM for anchor to scroll"
      pattern: "HistoriaSection"
---

<objective>
Build FooterSection.astro and wire all five components into index.astro. Run build verification and a human checkpoint to confirm the full page is functional.

Purpose: This plan closes Phase 3. After Plans 01 and 02 create the components in parallel, this plan assembles them into a working page, confirms the build passes, and gets human sign-off on the visual result.

Output: Complete wired index.astro with all Phase 3 sections, FooterSection.astro, and a successful build.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-content-sections/03-CONTEXT.md
@.planning/phases/03-content-sections/03-UI-SPEC.md
@.planning/phases/03-content-sections/03-RESEARCH.md

<!-- Prior plan outputs this plan depends on: -->
@.planning/phases/03-content-sections/03-01-SUMMARY.md
@.planning/phases/03-content-sections/03-02-SUMMARY.md

<interfaces>
<!-- Current index.astro (before this plan's modifications): -->
<!-- ---                                                         -->
<!-- import BaseLayout from '../layouts/BaseLayout.astro';       -->
<!-- import HeroCanvas from '../components/HeroCanvas.astro';    -->
<!-- ---                                                         -->
<!-- <BaseLayout>                                                 -->
<!--   <main>                                                     -->
<!--     <HeroCanvas />                                           -->
<!--   </main>                                                    -->
<!-- </BaseLayout>                                                -->

<!-- Target index.astro after this plan (from RESEARCH.md Pattern 4): -->
<!-- ---                                                                -->
<!-- import BaseLayout from '../layouts/BaseLayout.astro';              -->
<!-- import Nav from '../components/Nav.astro';                         -->
<!-- import HeroCanvas from '../components/HeroCanvas.astro';           -->
<!-- import HistoriaSection from '../components/HistoriaSection.astro'; -->
<!-- import UbicacionesSection from '../components/UbicacionesSection.astro'; -->
<!-- import MarqueeSection from '../components/MarqueeSection.astro';   -->
<!-- import FooterSection from '../components/FooterSection.astro';     -->
<!-- ---                                                                -->
<!-- <BaseLayout>                                                        -->
<!--   <Nav />                                                           -->
<!--   <main>                                                            -->
<!--     <HeroCanvas />                                                  -->
<!--     <HistoriaSection />                                             -->
<!--     <UbicacionesSection />                                          -->
<!--     <MarqueeSection />                                              -->
<!--   </main>                                                           -->
<!--   <FooterSection />                                                 -->
<!-- </BaseLayout>                                                       -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FooterSection.astro — brand footer with Instagram link, copyright, tagline</name>
  <files>src/components/FooterSection.astro</files>
  <read_first>
    - src/styles/global.css (confirm bg-surface, text-electric, text-on-surface token names)
    - referencias/juanaV1.html (footer structure reference — adapt class names to project tokens)
    - .planning/phases/03-content-sections/03-UI-SPEC.md (FooterSection component spec and Copywriting Contract)
  </read_first>
  <action>
Create `src/components/FooterSection.astro` with the following exact structure:

**Footer wrapper (per D-15, D-16, UI-SPEC FooterSection):**
The component renders a `<footer>` element (not `<section>`) — this will be placed outside `<main>` in index.astro for correct HTML semantics.

```html
<footer class="bg-surface w-full">
  <div class="flex flex-col md:flex-row justify-between items-center w-full px-8 py-12 gap-6 max-w-[1920px] mx-auto">

    <!-- Left block: brand logotype + copyright -->
    <div class="flex flex-col gap-2">
      <span class="font-display font-bold text-xl text-electric uppercase tracking-tighter">
        JUANA HOUSE
      </span>
      <span class="font-body text-[11px] uppercase tracking-widest text-on-surface/60">
        ©2024 JUANA HOUSE. URBAN NOMAD EDITORIAL.
      </span>
    </div>

    <!-- Center: Instagram link -->
    <a href="https://instagram.com/juana.onyourday"
       target="_blank"
       rel="noopener noreferrer"
       class="font-body text-[11px] uppercase tracking-widest text-on-surface/60 hover:text-electric transition-none">
      @juana.onyourday
    </a>

    <!-- Right block: establishment year + tagline chip -->
    <div class="flex flex-col items-end gap-2">
      <span class="font-display font-bold text-electric tracking-tighter uppercase text-[11px]">
        Established 2024
      </span>
      <span class="font-display font-bold text-[11px] text-on-surface/40 uppercase tracking-widest">
        URBAN NOMAD EDITORIAL
      </span>
    </div>

  </div>
</footer>
```

**Critical rules (per D-15, D-16, UI-SPEC FooterSection):**
- Component renders as `<footer>` element (not `<section>` or `<div>`) — semantic HTML.
- No `border-top` — section break from MarqueeSection's Electric Blue is sufficient (No-Line Rule per DESIGN.md).
- `text-xl` on brand logotype is a UI chrome exception (see UI-SPEC Typography Notes) — do NOT use for body content.
- `font-bold` (700) only — NEVER `font-black`.
- `transition-none` on Instagram link hover — instant color change per DESIGN.md "no slow transitions" rule.
- `©2024` (not `©2025` or `©${new Date().getFullYear()}`) — static hardcoded per Copywriting Contract.
- `bg-surface` (#131313) on footer background — same as page, no visual border needed.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb && grep -n "JUANA HOUSE" src/components/FooterSection.astro && grep -n "instagram.com/juana.onyourday" src/components/FooterSection.astro && grep -n "©2024 JUANA HOUSE" src/components/FooterSection.astro && grep -n "URBAN NOMAD EDITORIAL" src/components/FooterSection.astro && grep -n "bg-surface" src/components/FooterSection.astro && grep -n "text-electric" src/components/FooterSection.astro && echo "FooterSection checks passed"</automated>
  </verify>
  <done>
    - FooterSection.astro exists at src/components/FooterSection.astro
    - File contains: "JUANA HOUSE" with text-electric and font-display
    - File contains: href="https://instagram.com/juana.onyourday" target="_blank" rel="noopener noreferrer"
    - File contains: "©2024 JUANA HOUSE. URBAN NOMAD EDITORIAL."
    - File contains: "URBAN NOMAD EDITORIAL" tagline chip
    - File contains: bg-surface on footer element
    - File renders as <footer> element (not <section> or <div>)
    - No border-top or border-t anywhere in the file
    - No font-black class anywhere
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire index.astro — import all components and verify build</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (read current state before modifying — currently has BaseLayout + HeroCanvas only)
    - src/components/Nav.astro (confirm it exists from Plan 01)
    - src/components/HistoriaSection.astro (confirm it exists from Plan 01)
    - src/components/UbicacionesSection.astro (confirm it exists from Plan 02)
    - src/components/MarqueeSection.astro (confirm it exists from Plan 02)
    - src/components/FooterSection.astro (confirm it was just created in Task 1)
  </read_first>
  <action>
Replace the entire content of `src/pages/index.astro` with the following (per RESEARCH.md Pattern 4 and D-01 section order):

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import Nav from '../components/Nav.astro';
import HeroCanvas from '../components/HeroCanvas.astro';
import HistoriaSection from '../components/HistoriaSection.astro';
import UbicacionesSection from '../components/UbicacionesSection.astro';
import MarqueeSection from '../components/MarqueeSection.astro';
import FooterSection from '../components/FooterSection.astro';
---

<BaseLayout>
  <Nav />
  <main>
    <HeroCanvas />
    <HistoriaSection />
    <UbicacionesSection />
    <MarqueeSection />
  </main>
  <FooterSection />
</BaseLayout>
```

**Critical placement rules (per RESEARCH.md Pattern 4, Pitfall 5):**
- `<Nav />` is placed BEFORE `<main>` — Nav is `position: fixed` and semantically a site header, not flow content.
- `<FooterSection />` is placed AFTER `</main>` — FooterSection renders as a `<footer>` element which must be outside `<main>` for correct HTML semantics.
- Section order inside `<main>` matches D-01: HeroCanvas → HistoriaSection → UbicacionesSection → MarqueeSection.
- MarqueeSection goes inside `<main>` (it's content, not footer).

**After editing index.astro, run the build to verify all components compile:**
```bash
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb && npm run build 2>&1
```

If the build fails, read the error output carefully:
- Missing component import → check filename in src/components/ for typo
- Tailwind class not found → verify token name in global.css @theme (e.g., bg-electric not bg-primary)
- Font class error → confirm font-bold not font-black
- @keyframes conflict → move @keyframes from global.css to component <style> if duplicated

**Also run dev server smoke test after successful build:**
```bash
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb && npm run dev -- --host 0.0.0.0 &
sleep 3 && curl -s http://localhost:4321/ | grep -c "JUANA HOUSE"
```
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb && grep -n "import Nav" src/pages/index.astro && grep -n "import HistoriaSection" src/pages/index.astro && grep -n "import UbicacionesSection" src/pages/index.astro && grep -n "import MarqueeSection" src/pages/index.astro && grep -n "import FooterSection" src/pages/index.astro && npm run build 2>&1 | tail -5 && echo "index.astro wiring and build checks passed"</automated>
  </verify>
  <done>
    - src/pages/index.astro imports Nav, HeroCanvas, HistoriaSection, UbicacionesSection, MarqueeSection, FooterSection
    - <Nav /> is placed before <main> in the template
    - <FooterSection /> is placed after </main> in the template
    - Section order inside <main>: HeroCanvas → HistoriaSection → UbicacionesSection → MarqueeSection
    - npm run build completes without errors
    - dist/ directory exists after build
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual and functional verification of Phase 3</name>
  <what-built>
    Complete Phase 3 page with all 5 components wired:
    - Nav: fixed, Electric Blue logotype, anchor links to #historia and #ubicaciones, Instagram link
    - HeroCanvas: existing Three.js hero (unchanged)
    - HistoriaSection: editorial 12-col grid with "THE JUANA TRUCK®" heading and Spanish brand narrative
    - UbicacionesSection: two location cards with grayscale hover, Iron Man and Cara Sur
    - MarqueeSection: scrolling Electric Blue band with brand phrases
    - FooterSection: brand logotype, Instagram link, copyright

    Run locally: npm run dev in /Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb
  </what-built>
  <how-to-verify>
    1. Open http://localhost:4321/ in browser
    2. Confirm nav is visible at top with "JUANA HOUSE" in Electric Blue
    3. Scroll down past 80px — confirm nav background becomes frosted/dark (glassmorphism activates)
    4. Click "NUESTRA HISTORIA" in nav — confirm page scrolls to Historia section
    5. Click "UBICACIONES" in nav — confirm page scrolls to Ubicaciones section
    6. In Historia section: confirm "THE JUANA TRUCK®" heading and Spanish body copy is visible
    7. In Ubicaciones: hover over each card — confirm cards transition from grayscale to revealed
    8. Confirm Iron Man address reads "DEL BONO 383 SUR, SAN JUAN, ARGENTINA J5400"
    9. Confirm Cara Sur address reads "BARREAL, SAN JUAN, ARGENTINA"
    10. Confirm the Electric Blue marquee band is scrolling with brand phrases
    11. Footer: confirm "JUANA HOUSE" in Electric Blue, @juana.onyourday link, copyright text
    12. Resize to mobile (< 768px) — confirm nav center links are hidden, Historia grid stacks to single column
    13. If possible: enable OS reduced motion — confirm marquee stops scrolling
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- npm run build exits 0 with no errors
- All 6 component imports present in index.astro
- Nav placed before main, FooterSection placed after main
- Human confirmed: glassmorphism activates on scroll, anchor links work, grayscale hover works, marquee scrolls, mobile responsive layout correct
</verification>

<success_criteria>
- FooterSection.astro exists with JUANA HOUSE logotype, Instagram link, copyright ©2024, URBAN NOMAD EDITORIAL tagline
- index.astro has all 6 imports and correct semantic structure (Nav before main, FooterSection after main)
- npm run build completes without errors
- Human approves visual/functional review: nav glassmorphism, anchor navigation, grayscale card hover, marquee animation, footer content
- Phase 3 all requirements met: NAV-01, HIST-01, HIST-02, MARQ-01, MARQ-02, UBIC-01, UBIC-02, UBIC-03, FOOT-01
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-03-SUMMARY.md` following the summary template.
</output>
