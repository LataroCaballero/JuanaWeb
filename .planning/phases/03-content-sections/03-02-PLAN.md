---
phase: 03-content-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/UbicacionesSection.astro
  - src/components/MarqueeSection.astro
autonomous: true
requirements: [UBIC-01, UBIC-02, UBIC-03, MARQ-01, MARQ-02]

must_haves:
  truths:
    - "User can see Iron Man location: 'IRON MAN / SAN JUAN', 'DEL BONO 383 SUR, SAN JUAN, ARGENTINA J5400', 'CURRENT DOCK' chip"
    - "User can see Cara Sur location: 'CARA SUR / BARREAL', 'BARREAL, SAN JUAN, ARGENTINA', 'MOUNTAIN POST' chip"
    - "Both location cards have hours CTA linking to https://instagram.com/juana.onyourday"
    - "Location cards transition from grayscale to color on hover (CSS-only, no GSAP)"
    - "Marquee scrolls continuously with brand phrases on Electric Blue background"
    - "Marquee pauses when prefers-reduced-motion: reduce is active (CSS media query, no JS)"
  artifacts:
    - path: "src/components/UbicacionesSection.astro"
      provides: "Two-column location cards with id=ubicaciones, grayscale hover, correct addresses"
      exports: []
    - path: "src/components/MarqueeSection.astro"
      provides: "CSS-only marquee on bg-electric with prefers-reduced-motion support"
      exports: []
  key_links:
    - from: "UbicacionesSection.astro card image placeholder"
      to: "CSS group-hover filter"
      via: "grayscale group-hover:grayscale-0 transition-all duration-700 on placeholder div"
      pattern: "grayscale.*group-hover:grayscale-0"
    - from: "MarqueeSection.astro .marquee-track"
      to: "@keyframes marquee"
      via: "translateX(-50%) on doubled phrase set — 8 spans (4 phrases × 2 copies)"
      pattern: "translateX\\(-50%\\)"
---

<objective>
Build UbicacionesSection and MarqueeSection as standalone Astro components, ready to be imported into index.astro in Plan 03.

Purpose: Deliver the location discovery experience and brand identity stamp. Ubicaciones shows the two physical locations with hover interactions. MarqueeSection is the full-bleed Electric Blue band that acts as the visual pre-footer separator.

Output: `src/components/UbicacionesSection.astro` and `src/components/MarqueeSection.astro` — both CSS-only interactive, no GSAP, no external requests.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-content-sections/03-CONTEXT.md
@.planning/phases/03-content-sections/03-UI-SPEC.md
@.planning/phases/03-content-sections/03-RESEARCH.md

<interfaces>
<!-- Existing tokens from src/styles/global.css — use these utilities, never hex literals -->
<!-- @theme tokens available as Tailwind utilities: -->
<!--   bg-electric / text-electric → #0055ff -->
<!--   bg-surface → #131313 -->
<!--   bg-surface-high → #353534 (use as card image placeholder bg) -->
<!--   text-on-surface → #e5e2e1 -->
<!--   font-display → Space Grotesk (700 only — never font-black) -->
<!--   font-body → Work Sans (500) -->

<!-- Zero Roundedness: all border-radius = 0px (declared in @theme) -->
<!-- No-Line Rule: section breaks via background color shifts only, no border-top -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: UbicacionesSection.astro — two location cards with grayscale hover and Instagram CTA</name>
  <files>src/components/UbicacionesSection.astro</files>
  <read_first>
    - src/styles/global.css (confirm bg-surface, bg-surface-high, bg-electric token names and zero-radius @theme)
    - referencias/juanaV1.html (location card structure reference — adapt class names to project tokens)
    - .planning/phases/03-content-sections/03-UI-SPEC.md (UbicacionesSection component spec and Copywriting Contract)
  </read_first>
  <action>
Create `src/components/UbicacionesSection.astro` with the following exact structure:

**Section wrapper (per D-08, UI-SPEC UbicacionesSection):**
```html
<section id="ubicaciones" class="bg-surface">
  <div class="grid grid-cols-1 md:grid-cols-2">
```
- No max-width constraint — edge-to-edge as per D-08.
- Section `id="ubicaciones"` required for anchor-scroll nav.

**Card template (apply to BOTH cards — only content differs):**
```html
<div class="relative h-[600px] group overflow-hidden">
  <!-- Phase 3: placeholder div. Phase 4: replace with <Image /> and keep grayscale classes. -->
  <div class="bg-surface-high w-full h-full grayscale group-hover:grayscale-0 transition-all duration-700"></div>

  <!-- Electric Blue overlay — cleared on hover to reveal card color -->
  <div class="absolute inset-0 bg-electric/20 mix-blend-multiply group-hover:bg-transparent transition-all duration-700"></div>

  <!-- Bottom text block -->
  <div class="absolute bottom-0 left-0 p-12">
    <!-- Label chip -->
    <span class="bg-black text-white px-4 py-1 text-[11px] font-display font-bold tracking-widest uppercase mb-4 block w-fit">
      {CHIP_TEXT}
    </span>

    <!-- Card heading -->
    <h3 class="font-display font-bold text-[60px] text-white uppercase leading-none tracking-tighter">
      {HEADING}
    </h3>

    <!-- Address -->
    <p class="text-white/80 font-body font-bold mt-4 uppercase tracking-[0.2em] text-[11px]">
      {ADDRESS}
    </p>

    <!-- Hours CTA (no hardcoded hours per D-09) -->
    <a href="https://instagram.com/juana.onyourday"
       target="_blank"
       rel="noopener noreferrer"
       class="text-white/60 font-body text-[11px] uppercase tracking-widest mt-2 block hover:text-electric transition-none">
      Ver horarios en @juana.onyourday
    </a>
  </div>
</div>
```

**Iron Man card content (per D-08, D-11, REQUIREMENTS.md UBIC-01, Copywriting Contract):**
- CHIP_TEXT: `CURRENT DOCK`
- HEADING: `IRON MAN / SAN JUAN`
- ADDRESS: `DEL BONO 383 SUR, SAN JUAN, ARGENTINA J5400`

**Cara Sur card content (per D-08, D-11, REQUIREMENTS.md UBIC-01, Copywriting Contract):**
- CHIP_TEXT: `MOUNTAIN POST`
- HEADING: `CARA SUR / BARREAL`
- ADDRESS: `BARREAL, SAN JUAN, ARGENTINA`

**Critical rules (per RESEARCH.md pitfalls and UI-SPEC):**
- Section `id="ubicaciones"` is required for anchor-scroll nav link — do NOT omit.
- `grayscale group-hover:grayscale-0 transition-all duration-700` on the placeholder div — this is CSS-only per D-10, NOT GSAP.
- `transition-all duration-700` (700ms) is the intentional "cinematic" duration per UI-SPEC Interaction Contracts — do NOT shorten.
- Address for Iron Man MUST be "DEL BONO 383 SUR, SAN JUAN, ARGENTINA J5400" — the reference HTML has a wrong address (Av. Libertador 1250). Per D-08 and REQUIREMENTS.md UBIC-01.
- No hardcoded hours — the hours CTA "Ver horarios en @juana.onyourday" is the confirmed content pattern per D-09.
- `font-body` class (Work Sans) on address and CTA text.
- `font-display font-bold` (Space Grotesk 700) on label chips and headings — NEVER `font-black`.
- Both cards use `group` on the outer div — Tailwind's `group` utility enables `group-hover:` on children.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb && grep -n 'id="ubicaciones"' src/components/UbicacionesSection.astro && grep -n "CURRENT DOCK" src/components/UbicacionesSection.astro && grep -n "MOUNTAIN POST" src/components/UbicacionesSection.astro && grep -n "DEL BONO 383 SUR" src/components/UbicacionesSection.astro && grep -n "BARREAL, SAN JUAN" src/components/UbicacionesSection.astro && grep -n "grayscale group-hover:grayscale-0" src/components/UbicacionesSection.astro && grep -n "instagram.com/juana.onyourday" src/components/UbicacionesSection.astro && echo "UbicacionesSection checks passed"</automated>
  </verify>
  <done>
    - UbicacionesSection.astro exists at src/components/UbicacionesSection.astro
    - File contains: id="ubicaciones" on outer section element
    - File contains: "CURRENT DOCK" and "MOUNTAIN POST" label chips
    - File contains: "IRON MAN / SAN JUAN" and "CARA SUR / BARREAL" headings
    - File contains: "DEL BONO 383 SUR, SAN JUAN, ARGENTINA J5400" (exact string — NOT Av. Libertador)
    - File contains: "BARREAL, SAN JUAN, ARGENTINA"
    - File contains: grayscale group-hover:grayscale-0 transition-all duration-700
    - File contains: bg-electric/20 mix-blend-multiply group-hover:bg-transparent transition-all duration-700
    - File contains: href="https://instagram.com/juana.onyourday" on CTA link (both cards)
    - No external image URLs in src attributes
    - No font-black class anywhere
    - No GSAP references
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MarqueeSection.astro — CSS-only marquee on Electric Blue with reduced-motion support</name>
  <files>src/components/MarqueeSection.astro</files>
  <read_first>
    - referencias/juanaV1.html (marquee CSS animation reference — lines 245-252 for @keyframes marquee)
    - src/styles/global.css (confirm bg-electric token name, font-display)
    - .planning/phases/03-content-sections/03-UI-SPEC.md (MarqueeSection component spec and Copywriting Contract)
  </read_first>
  <action>
Create `src/components/MarqueeSection.astro` with the following exact structure:

**Section and marquee track (per D-12, D-13, D-14, UI-SPEC MarqueeSection):**
```html
<section class="bg-electric py-12 overflow-hidden whitespace-nowrap">
  <div class="marquee-track flex font-display font-bold text-8xl text-white/20 uppercase tracking-tighter select-none">
    <!-- First copy of phrase set -->
    <span class="mx-8">JUANA HOUSE</span>
    <span class="mx-8">JUANATRUCK®</span>
    <span class="mx-8">NOMAD SOUL</span>
    <span class="mx-8">COFFEE ON YOUR WAY</span>
    <!-- Second copy — required for seamless translateX(-50%) loop (per RESEARCH.md Pitfall 4) -->
    <span class="mx-8">JUANA HOUSE</span>
    <span class="mx-8">JUANATRUCK®</span>
    <span class="mx-8">NOMAD SOUL</span>
    <span class="mx-8">COFFEE ON YOUR WAY</span>
  </div>
</section>
```

**Scoped `<style>` block (placed after HTML):**
```html
<style>
  .marquee-track {
    animation: marquee 20s linear infinite;
  }
  @keyframes marquee {
    0%   { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
  @media (prefers-reduced-motion: reduce) {
    .marquee-track {
      animation-play-state: paused;
    }
  }
</style>
```

**Critical rules (per D-12, D-13, D-14, RESEARCH.md pitfalls):**
- Phrases MUST appear EXACTLY TWICE in the DOM — 8 total `<span>` elements (4 phrases × 2 copies). The `translateX(-50%)` keyframe math only produces a seamless loop when content is exactly doubled. See RESEARCH.md Pitfall 4.
- `animation-play-state: paused` via `@media (prefers-reduced-motion: reduce)` CSS query — NO JavaScript needed (per D-13, MARQ-02).
- `20s linear infinite` animation duration — per D-12 default.
- `text-white/20` (white at 20% opacity) on `bg-electric` — creates the muted poster effect per D-14.
- `text-8xl` (96px Display token) on phrase text — per UI-SPEC MarqueeSection.
- `mx-8` (32px horizontal margin) as separator between phrases — per UI-SPEC Spacing Scale Exception.
- `font-bold` (700) — NEVER `font-black`.
- No `@keyframes` in global.css — define in scoped `<style>` block in this component to keep concerns local.
- No GSAP — this is pure CSS per D-12 and the Phase 3 boundary (GSAP is Phase 4).
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/JuanaWeb && grep -n "bg-electric" src/components/MarqueeSection.astro && grep -c "JUANA HOUSE" src/components/MarqueeSection.astro | grep -q "2" && grep -n "translateX(-50%)" src/components/MarqueeSection.astro && grep -n "prefers-reduced-motion" src/components/MarqueeSection.astro && grep -n "animation-play-state: paused" src/components/MarqueeSection.astro && grep -n "JUANATRUCK" src/components/MarqueeSection.astro && echo "MarqueeSection checks passed"</automated>
  </verify>
  <done>
    - MarqueeSection.astro exists at src/components/MarqueeSection.astro
    - File contains: bg-electric on outer section
    - File contains: "JUANA HOUSE" exactly 2 times (doubled phrase set)
    - File contains: "JUANATRUCK®", "NOMAD SOUL", "COFFEE ON YOUR WAY" each exactly 2 times
    - File contains: translateX(-50%) in @keyframes marquee
    - File contains: 20s linear infinite animation duration
    - File contains: @media (prefers-reduced-motion: reduce) block
    - File contains: animation-play-state: paused in reduced-motion block
    - File contains: text-white/20 on marquee track
    - No JS in this component (no <script> tag)
    - No GSAP references
    - No font-black class
  </done>
</task>

</tasks>

<verification>
Both components exist and pass structural grep checks. The build and visual verification happens in Plan 03 after all components are wired into index.astro.
</verification>

<success_criteria>
- src/components/UbicacionesSection.astro exists with id="ubicaciones", two cards, correct addresses (Del Bono 383 Sur not Av. Libertador), grayscale hover CSS, Instagram CTA links
- src/components/MarqueeSection.astro exists with bg-electric section, doubled phrase set (8 spans), translateX(-50%) keyframes, prefers-reduced-motion pause rule
- Both files contain zero external image/URL requests
- Both files contain zero GSAP references
- All copy matches Copywriting Contract from UI-SPEC exactly
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-02-SUMMARY.md` following the summary template.
</output>
